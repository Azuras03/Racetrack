let num_tiles_x = 45;
let num_tiles_y = 25;
let trackDensity = 2; // Densité, donc en gros pour une grille, on a densité^2 éléments dans une tile


export const utils = {
    num_tiles_x,
    num_tiles_y,
    trackDensity,
}